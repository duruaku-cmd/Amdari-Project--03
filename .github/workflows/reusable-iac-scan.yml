# Reusable stub: IaC Scan (Checkov/tfsec) + OPA - Day 17.
# Day 15 scaffold — the real gate logic is added on the day noted above.
# Until then this stub SUCCEEDS but prints what it will enforce, so the
# orchestrator wiring and branch-protection required-checks can be set up now.
name: reusable-iac-scan

on:
  workflow_call:

permissions:
  contents: read

jobs:
  iac-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: |
          echo "STAGE: IaC Scan (Checkov/tfsec) + OPA - Day 17"
          echo "This stage will run as a BLOCKING gate with a tuned severity threshold."
          echo "Scaffold OK."
